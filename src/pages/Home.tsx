import React from 'react';
import styled from '@emotion/styled';

const Home: React.FC = () => {
  return (
    <Wrapper>
      welcome
    </Wrapper>
  )
}

const Wrapper = styled.section`
  
`

export default Home;
